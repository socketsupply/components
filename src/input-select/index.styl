input-select
  select
    font 14px var(--monospace)
    padding 10px
    background-color var(--window)
    border-radius 2px
    border 1px solid var(--border)
    outline none
    -webkit-appearance none
    -moz-appearance none
    appearance none
