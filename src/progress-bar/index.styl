progress-bar
  display block

  .wrapper
    background-color var(--background)
    position relative

    .progress
      background-color var(--accent)
      width 0%
      height 100%
      transition width 0.2s ease
